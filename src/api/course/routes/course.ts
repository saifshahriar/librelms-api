/**
 * course routes: default core REST is intentionally disabled.
 * All app traffic goes through the contract-mirror routes in
 * api::platform (src/api/platform/routes/platform.ts).
 */
export default {
	routes: [],
};
